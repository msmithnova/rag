---
scenarios:
  - &test
    match: "Test"

features:
  - &step2-1
    FEATURE: features/test.feature
    weight: 0.25
    if_pass:
      - &step2-2
        FEATURE: features/test.feature
        version: 10
        weight: 0.25
        desc: "feature = something"
        failures:
          - *test
